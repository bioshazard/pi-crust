# SDK

Opinionated helpers for consistent use of the Crust kernel. `createCrustSdk()` roots
artifact storage, composition identity, and receipt journals. `NaturalStopAgent`
and its Pi adapter provide optional bounded execution without introducing execution
or interaction concepts into the kernel.
